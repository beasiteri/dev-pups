{
  "name": "dev-pups",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "packages": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run index.ts"
 },

  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^10.0.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
